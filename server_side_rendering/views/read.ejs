<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <nav
        class="mb-5 text-white bg-zinc-700 p-5 rounded-lg flex flex-wrap gap-3"
      >
        <img src="/images/logo.png" class="w-10 h-10 rounded-full" />
        <a
          href="/"
          class="text-1xl px-5 py-2 bg-zinc-600 rounded-lg hover:bg-white hover:duration-500 hover:text-black"
          >Home</a
        >
      </nav>

      <h1 class="text-3xl mb-3">All users</h1>
      <% if(users.length > 0) { %> 
      <div class="users flex gap-3 flex-wrap">
        <% users.forEach(function(user){ %>
        <div class="user max-w-72 p-4 bg-zinc-800 rounded-lg">
            <img src="  <%= user.imageUrl %> " alt="user image" class="rounded-lg">
          <h3 class="mt-3 text-2xl"><%= user.username %></h3>
          <h2 class="mt-1 text-1xl"><%= user.email %></h2>
          <div class="justify-between flex mt-4 text-zinc-500 ">
            <div class="hover:text-zinc-200">
              <a href="/edit/<%= user._id %> " class="hover:text-white">Edit User</a>
            </div>
            <div class="hover:text-zinc-200">
              <a href="/delete/<%= user._id %> " class="hover:text-red-500">Delete User</a>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } else { %>
            <h3 class="text-zinc-500">No users yet.</h3>
      <% } %> 
    </div>
  </body>
</html>
